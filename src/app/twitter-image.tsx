/**
 * Twitter card image — reuses the OG image. Same 1200×630 mono
 * editorial card for every social surface. Diverge into its own
 * design only if Twitter renders something visibly different.
 */
export { alt, size, contentType } from "./opengraph-image";
export { default } from "./opengraph-image";
