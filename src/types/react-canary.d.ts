/// <reference types="react/canary" />

/*
 * Activates React's canary type surface — needed for the
 * `ViewTransition` component export used in app/layout.tsx.
 * The runtime export exists in the React copy Next.js vendors;
 * @types/react only declares it behind this reference.
 */
